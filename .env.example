# Google Cloud API key with the Calendar API enabled, restricted to
# HTTP referrers for this site's domain(s). Used client-side to read
# the public practice-schedule calendar — see src/useSchedule.ts.
VITE_GOOGLE_CALENDAR_API_KEY=
